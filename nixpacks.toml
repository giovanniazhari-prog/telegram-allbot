[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python3"]

[phases.install]
cmds = [
  "python3 -m pip install -U yt-dlp --break-system-packages",
  "npm install"
]

[start]
cmd = "bash startup.sh"
